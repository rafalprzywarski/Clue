* refactor clue.compiler.translate_call to take a list (or a vector) instead of ...
* vars
* meta for vars
* ' and quote
* bindings
* ~ and unquote
* ~@ and unquote-splicing
* syntax-quote `
* defmacro
* protocols
